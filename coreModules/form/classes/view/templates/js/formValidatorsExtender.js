//
// Este fichero se puede sobreescribir en la app para añadir nuevos metodos sin perder soporte del modulo
//

// // SAMPLE METHOD
// $.validator.addMethod(
//   "validatorName",
//   function( value, element ) {
//     return ( value==='' && this.optional( element ) ) || /^\d+$/.test( value );
//   },
//   __("Only digits (1234567890)")
// );

